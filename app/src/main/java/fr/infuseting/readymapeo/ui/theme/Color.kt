package fr.infuseting.readymapeo.ui.theme

import androidx.compose.ui.graphics.Color

// ── Palette principale (tons bleu/indigo modernes) ──────────
val Primary = Color(0xFF4F46E5)          // Indigo vif
val PrimaryVariant = Color(0xFF3730A3)    // Indigo plus foncé
val Secondary = Color(0xFF06B6D4)         // Cyan
val SecondaryVariant = Color(0xFF0891B2)

// ── Surfaces ────────────────────────────────────────────────
val Background = Color(0xFFF8FAFC)
val Surface = Color(0xFFFFFFFF)
val SurfaceVariant = Color(0xFFF1F5F9)

// ── Texte ───────────────────────────────────────────────────
val OnPrimary = Color(0xFFFFFFFF)
val OnSecondary = Color(0xFFFFFFFF)
val OnBackground = Color(0xFF1E293B)
val OnSurface = Color(0xFF334155)
val OnSurfaceVariant = Color(0xFF64748B)

// ── Statuts ─────────────────────────────────────────────────
val Success = Color(0xFF10B981)
val Warning = Color(0xFFF59E0B)
val Error = Color(0xFFEF4444)
val OfflineBanner = Color(0xFFFEF3C7)      // Fond bannière hors-ligne
val OfflineText = Color(0xFF92400E)         // Texte bannière hors-ligne

// ── Dark Mode ───────────────────────────────────────────────
val DarkPrimary = Color(0xFF818CF8)
val DarkBackground = Color(0xFF0F172A)
val DarkSurface = Color(0xFF1E293B)
val DarkOnBackground = Color(0xFFE2E8F0)
val DarkOnSurface = Color(0xFFCBD5E1)